server {

	listen 80;

    location /static {
        autoindex on;
        expires -1;
        alias  /var/www/static/;
    }

    location /media {
        autoindex on;
       	expires -1;
        alias /var/www/media/;
    }

    location / {
		proxy_pass http://web:8000;
	}

}
